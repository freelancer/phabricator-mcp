# Optional: The server automatically reads from ~/.arcrc if these are not set

# Phabricator instance URL (without trailing slash)
PHABRICATOR_URL=https://phabricator.example.com

# Conduit API token (starts with api- or cli-)
PHABRICATOR_API_TOKEN=api-xxxxxxxxxxxxx
